services:
  - type: worker
    name: telegram-trigger-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python 123.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
